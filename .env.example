# Connect any provider endpoint that accepts { email, source } as JSON.
# Without this value the site reports that signup is in preview mode.
NEWSLETTER_WEBHOOK_URL=

# Public production origin, used for metadata and feeds.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
